name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      content: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup
        uses: mlugg/setup-zig@v1
        with:
          version: master

      - name: Build
        run: zig build release -Doptimize=ReleaseFast

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: zig-out/bin/zon2tct-*
          generate_release_notes: true
